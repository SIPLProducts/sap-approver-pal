# Shared: WebSocket / SSE upgrade map. Sends "upgrade" only when the client
# actually requested it, so plain HTTP keep-alive is not broken.
# Link to /etc/nginx/conf.d/00-upgrade-map.conf
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
