# ---------------------------------------------------------------------------
# Supabase self-hosted stack — PRODUCTION
# Copy to /data/webapplication/resl_approval/Production/backend/.env (chmod 600)
# Generate EVERY secret yourself; do not reuse the Quality values.
#
# Port map (Production): Kong 8010 / 8453, Studio 3100, Postgres 5442,
# pooler 6553. Quality keeps 8000 / 8443 / 3000 / 5432 / 6543.
# ---------------------------------------------------------------------------

############ Secrets — generate fresh for Production ############
POSTGRES_PASSWORD=CHANGE_ME_openssl_rand_hex_24
# openssl rand -hex 32 ; then mint ANON_KEY / SERVICE_ROLE_KEY from THIS secret.
JWT_SECRET=CHANGE_ME_openssl_rand_hex_32
ANON_KEY=CHANGE_ME_minted_jwt_role_anon
SERVICE_ROLE_KEY=CHANGE_ME_minted_jwt_role_service_role
SECRET_KEY_BASE=CHANGE_ME_openssl_rand_hex_32
VAULT_ENC_KEY=CHANGE_ME_openssl_rand_hex_16
LOGFLARE_PUBLIC_ACCESS_TOKEN=CHANGE_ME_openssl_rand_hex_32
LOGFLARE_PRIVATE_ACCESS_TOKEN=CHANGE_ME_openssl_rand_hex_32

############ Studio login ############
DASHBOARD_USERNAME=studioadmin
DASHBOARD_PASSWORD=CHANGE_ME_openssl_rand_hex_24
STUDIO_DEFAULT_ORGANIZATION=RESL
STUDIO_DEFAULT_PROJECT=RESL Approvals Production

############ Database ############
POSTGRES_HOST=db
POSTGRES_DB=postgres
POSTGRES_PORT=5432

############ Host ports (PRODUCTION) ############
POSTGRES_PORT_EXT=5442
KONG_HTTP_PORT=8010
KONG_HTTPS_PORT=8453
STUDIO_PORT=3100
POOLER_PROXY_PORT_TRANSACTION=6553
POOLER_DEFAULT_POOL_SIZE=20
POOLER_MAX_CLIENT_CONN=200
POOLER_TENANT_ID=resl-production
POOLER_DB_POOL_SIZE=5

############ Public URLs — must match the nginx vhost ############
SITE_URL=http://10.150.150.130:9091
API_EXTERNAL_URL=http://10.150.150.130:8010
SUPABASE_PUBLIC_URL=http://10.150.150.130:8010
ADDITIONAL_REDIRECT_URLS=http://10.150.150.130:9091/**

############ Auth policy ############
DISABLE_SIGNUP=true
ENABLE_EMAIL_SIGNUP=true
ENABLE_EMAIL_AUTOCONFIRM=false
ENABLE_ANONYMOUS_USERS=false
ENABLE_PHONE_SIGNUP=false
ENABLE_PHONE_AUTOCONFIRM=false
JWT_EXPIRY=3600

############ SMTP ############
SMTP_ADMIN_EMAIL=no-reply@example.com
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=CHANGE_ME
SMTP_PASS=CHANGE_ME
SMTP_SENDER_NAME=RESL Approvals
MAILER_URLPATHS_INVITE=/login
MAILER_URLPATHS_CONFIRMATION=/login
MAILER_URLPATHS_RECOVERY=/login
MAILER_URLPATHS_EMAIL_CHANGE=/login

############ Storage ############
FILE_SIZE_LIMIT=52428800
STORAGE_BACKEND=file
GLOBAL_S3_BUCKET=stub
REGION=stub

############ Misc ############
PGRST_DB_SCHEMAS=public,storage,graphql_public
FUNCTIONS_VERIFY_JWT=true
IMGPROXY_ENABLE_WEBP_DETECTION=true
DOCKER_SOCKET_LOCATION=/var/run/docker.sock
